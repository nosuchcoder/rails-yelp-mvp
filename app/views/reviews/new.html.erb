<div class="container">
  <div class="row">
    <div class="col">
      <%= simple_form_for [@restaurant, @review] do |f| %>
      <%= f.input :content, placeholder: " Content"%>
      <%= f.input :rating, placeholder: " Score"%>
      <%= f.submit "Post review", class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</div>